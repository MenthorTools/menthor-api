buildscript {
	repositories {
		jcenter()
		mavenCentral()
	}
}

group = "net.menthor"
version = "1.0.1"
description = "Menthor - OntoUML API with EA Support"

apply plugin: 'groovy'
apply plugin: 'idea'

repositories {
	mavenCentral()
}

task fatJar(type: Jar) {
	baseName = "net.menthor."+project.name+"-all"
	from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
	with jar
}

dependencies {
	compile project(":ontouml")
	compile "org.codehaus.groovy:groovy-all:2.4.6"
}

jar {
	exclude "META-INF/*.SF"
	exclude "META-INF/*.DSA"
	exclude "META-INF/*.RSA"
}

task wrapper(type: Wrapper) {
	gradleVersion = '2.7'
}
